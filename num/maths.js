const score=400

console.log(score);

const balance =new Number(100) // its a number.

console.log(balance);

 console.log(balance.toString().length);// converted into string or finding into length

 console.log(balance.toFixed(1)); // converted into float value..

 const anothernum = 2312.22
 console.log(anothernum.toPrecision(3));
  // for using precision is for exponnetional; o/p is 2.31e+3...3rd index is precision or other is expo...
  const num2=1000000000
  console.log(num2.toLocaleString()); // use for comma in Zero's
    console.log(num2.toLocaleString('en-IN'));// in idian style every two Zero use comma,,..

   ////// MAths..................
    
console.log(Math);
console.log(Math.abs(-90));// the value is o/p== in positive..
console.log(Math.round(7.09));// round figure Value is 7
 console.log(Math.max(1,78,9,0,7)); //find max value..

 console.log(Math.min(9,8,4,5,));// find minimum value...
console.log(Math.random())// print random no in b/w (0-1)
console.log(Math.random()*10+1)// print <1..







